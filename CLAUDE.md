@AGENTS.md

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `dev-jasp/vpdrl-inventory-system`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Design reference

The UI is designed in Claude Design and vendored to `design/LabTrack Dashboard.dc.html`. Read it before writing UI code. See `docs/agents/design.md`.
