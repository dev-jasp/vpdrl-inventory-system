// The dashboard itself, which the catch-all beside this cannot match: a
// catch-all needs a segment, and `/` has none. Without it, dismissing a dialog
// by way of the sidebar's Dashboard link would leave it hanging over `/`.

export default function ModalHome() {
  return null;
}
