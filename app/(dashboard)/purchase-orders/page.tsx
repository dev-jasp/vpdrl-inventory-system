// Purchase Orders — in the design this is the inventory list scoped to items
// below their reorder point. Kept as its own route because it is a distinct
// sidebar entry and will diverge from Inventory once orders are real.
// TODO: build from `LabTrack Dashboard.dc.html`.

export default function PurchaseOrdersPage() {
  return <div>Purchase Orders</div>;
}
