// Suppliers — contact, item count, on-time delivery, lead time. Add/edit
// supplier is a dialog, not a route.
// TODO: build from `LabTrack Dashboard.dc.html` — components/suppliers/.

export default function SuppliersPage() {
  return <div>Suppliers</div>;
}
