// Item detail — stock position vs. reorder point, lots table, withdrawals
// chart, activity log, specification, item photo.
// TODO: build from `LabTrack Dashboard.dc.html` — components/inventory/.

export default function InventoryItemPage() {
  return <div>Item detail</div>;
}
