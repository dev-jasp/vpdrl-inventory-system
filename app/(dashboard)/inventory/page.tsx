// Inventory list — list/grid toggle, sortable table, pagination.
// The sidebar's Chemicals / Equipment / Low stock / Expiring-due entries are
// this same page filtered via searchParams (cat, flag, zone, q, sort), not
// separate routes.
// TODO: build from `LabTrack Dashboard.dc.html` — components/inventory/.

export default function InventoryPage() {
  return <div>Inventory</div>;
}
