// Add item form — category, quantities, reorder point, expiry/calibration
// date, handling notes, photo capture.
// TODO: build from `LabTrack Dashboard.dc.html` — components/inventory/.

export default function NewInventoryItemPage() {
  return <div>Add item</div>;
}
