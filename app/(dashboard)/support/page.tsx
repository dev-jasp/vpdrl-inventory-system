// Support — FAQ accordion, contact channels, system status.
// TODO: build from `LabTrack Dashboard.dc.html` — components/support/.

export default function SupportPage() {
  return <div>Support</div>;
}
